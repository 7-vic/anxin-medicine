# Design System — 安心用药

> 项目唯一设计真相来源。V0.2 起用，每次设计/审查先读它，偏离规范必须显式记录理由。

## 1. Product Context（产品上下文）

- **这是什么：** 用药执行助手 App——帮老人理解、执行、回忆医生/药师医嘱，不是闹钟或清单。
- **为谁：** A 老人（第一优先级，解决真实用药执行）→ B 家属（协助与关注）。
- **行业/空间：** 老年健康/慢病管理。精神对标：医院处方笺的"清晰可执行" + 家庭药盒的"触手可及"。
- **项目类型：** 移动端 App（390×844 设计稿基准）。

## 2. Aesthetic Direction（美学方向）

- **方向：** 「温暖处方笺」——处方单式的清晰执行感 + 家庭早晨的暖意。
- **气质：** 清晰、安心、可信、不冷漠。
- **参考对象：** 医院处方/药品说明书的信息层级；早晨阳光下的药盒；微信长辈模式的字号。
- **要避免的默认陷阱：** 紫色渐变（医疗≠科技感）、通用圆角卡片堆阴影、emoji 当图标、信息密度过高（老人看不完）。

## 3. Typography（字体）

- **显示/标题：** **MiSans**（圆润现代，CDN：jsdelivr misans-webfont）→ 兜底 HarmonyOS Sans SC / OPPO Sans / 系统。字重 600-700。
- **正文：** 同栈，字重 400。
- **数据/标签：** 同栈，字重 500（数字用 tabular-nums 防跳动）。
- **字阶表：** Hero 40 / H1 28 / H2 22 / Body 17 / Small 15 / Caption 13。
  - **偏离说明**：比常规 App 字阶大一号——目标用户是老人，视力下降是默认前提，大字是产品需求不是装饰。
- **行高/字重倾向：** 正文 1.5，标题 1.25。

## 4. Color（色彩）

- **中性色：** 背景 `#F7F6F2`（暖白，早晨感）/ 表面 `#FFFFFF` / 边框 `#E5E3DC` / 文字主 `#232323` / 次 `#6B6B6B`。
- **主色：** `#1D6BA8`（医疗蓝，信任感）——用于当前任务、主按钮、选中态。
- **强调色：** `#E8862D`（药丸橙，唤醒/提醒）——用于"现在该吃药"标签、提醒铃。
- **语义色：** 成功 `#2F7D4F`（已吃确认）/ 警告 `#E8862D` / 错误 `#C0392B`。
- **配色逻辑：** 60% 暖白底 / 30% 深灰蓝文字 / 10% 蓝+橙点缀。对比度全部 ≥ 4.5:1（老人可读），主按钮对比度 ≥ 7:1。

## 5. Spacing（间距）

- **基准：** 8pt 网格。
- **例外及理由：** 任务卡内部用 16-24pt（大卡片、大按钮，老人拇指+视觉都需宽松）；行高宽松。**老人产品，留白是功能不是浪费。**

## 6. Layout（布局）

- **网格/边距：** 页面边距 20px，卡片圆角 20px，卡片间距 16px。
- **断点：** 移动 390 基准；不做桌面（V1 前）。
- **布局原则：** 从上到下"当前任务 → 今日计划 → 药盒区"；单一焦点（一次只引导一个动作）；卡片化但不堆多层阴影（1px 边框 + 极浅阴影）。

## 7. Components（组件）

| 组件 | 正常态 | 空态 | 加载态 | 错误态 | 说明 |
|---|---|---|---|---|---|
| 当前任务卡 | 药名+粒数+两按钮 | 今日无任务→"今天没有要吃的药" | 骨架屏（灰块） | 数据加载失败→重试 | 页面最大元素，Hero 字号 |
| 计划行 | 时间+药名+状态 | 同卡片空态 | 骨架 | 同卡片 | 已完成置灰打勾 |
| 药盒图 | 圆角药盒缩略图 | 无图→文字名 | 占位灰块 | 加载失败→文字兜底 | 图+大字号名称并行 |
| 主按钮「我吃好了」 | 主色实心 56px 高 | — | 按下变深 | 确认失败→toast+重试 | 拇指热区中心偏下 |
| 次按钮「再说一遍」 | 描边按钮 | — | — | 语音失败→提示重试 | 紧邻主按钮，45px |
| 底部 Tab | 4 项：首页/AI/家属/我的 | — | — | — | 选中态主色；**线框 SVG 图标**（Feather 风格，stroke 1.8，随选中态变色） |

## 8. Motion（动效）

- **动效意图：** 语音播报时任务卡轻微呼吸高亮（引导注意）；确认后任务卡渐隐、下一任务出现。
- **时长/缓动倾向：** 150-250ms，ease-out。
- **减少动效偏好：** 支持 `prefers-reduced-motion` 关闭呼吸动画。
